{% extends 'base.html' %} {% block content %} {% include 'navbar.html' %} 

<h1>{{ 'Search Results' }}</h1>
        <div class="row">
            {% for d in details %}
            <div class="media">
              <img class="account-img" src="{{d[1]}}">
              <div class="media-body">
                <p><a href="{{url_for('main.account', username=d[0].username)}}">{{d[0].username}}</a></p>
            <!-- <div class="card" style="width: 14rem;">
                <img src="{{d[1]}}" class="card-img-top" alt="...">
                
            </div> -->
          </div>
        </div>
            {% endfor %}
        </div>
        <style>
          .account-img{
            height: 130px;
            width: 130px;
            border-radius: 50%;
          }
          p{
            position: absolute;
            font-size: 34px;
            font-weight: 800;
            top: 20%;
            left: 23%;
          }
        </style>


{% endblock content %}
