<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/home">Instagram</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <% if (logginUser) { %>
            <% if (session.notificationLength == null || session.notificationLength == 0) { %>
            <a class="nav-link" href="/notifications">Notifications
              <span class="badge badge-secondary" id="message_count" style=""></span>
          </a>
            <% } else { %>
              <a class="nav-link" href="/notifications">Notifications
                <span class="badge badge-secondary" id="message_count" style=""><%= session.notificationLength %></span>
            </a>
            <% } %>
            <% } %>
          </li>
          
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fa fa-user" aria-hidden="true"></i> User Management
            </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <% if (login) { %>
                  <a class="dropdown-item" href="/<%= logginUser.username %>"></i>User Account</a>
                  <a class="dropdown-item" href="/logout">Logout</a>
                <% } else { %>
                  <a class="dropdown-item" href="/signup"></i>Signup</a>
                  <a class="dropdown-item" href="/signin"></i>Signin</a>
                <% } %>
              </div>
        </li>
      
      </ul>
  
    </div>
  </nav>

